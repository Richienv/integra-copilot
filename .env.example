# Any OpenAI-compatible API. Alibaba Cloud Bailian (Qwen) shown; DeepSeek works the same way.
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=sk-your-key
LLM_MODEL=qwen-plus
# Optional: price per million tokens, so evaluation reports can show cost.
LLM_PRICE_IN=
LLM_PRICE_OUT=
# Optional: a real Integra database, connecting as copilot_reader (see README, "Against a real Integra database").
COPILOT_DATABASE_URL=
# No API key? Leave LLM_API_KEY out of your environment: with the ChatGPT app installed and signed in,
# the copilot uses ChatGPT through the Codex CLI instead. To force it and pick the model:
# LLM_BACKEND=codex
# CODEX_MODEL=gpt-6-sol
# CODEX_EFFORT=low
